package com.medcore.app.ui.theme

import androidx.compose.ui.graphics.Color

// === MedCore Dark Theme Palette ===

// Backgrounds
val BackgroundDeep     = Color(0xFF04080F)   // near-black deep space
val BackgroundSurface  = Color(0xFF0B1120)   // dark navy surface
val BackgroundCard     = Color(0xFF111C2E)   // card background
val BackgroundElevated = Color(0xFF162038)   // elevated surfaces / sheets

// Accent — Electric Cyan (primary brand)
val CyanCore       = Color(0xFF00E5FF)
val CyanDim        = Color(0xFF00B8D4)
val CyanSubtle     = Color(0x2600E5FF)       // 15% opacity glow

// Accent — Indigo (secondary / interactive)
val IndigoCore     = Color(0xFF5B6DFF)
val IndigoDim      = Color(0xFF3D4FD6)
val IndigoSubtle   = Color(0x265B6DFF)

// Accent — Teal (progress / success)
val TealCore       = Color(0xFF00BFA5)
val TealDim        = Color(0xFF008F7A)
val TealSubtle     = Color(0x2600BFA5)

// Accent — Amber (warning / locked)
val AmberCore      = Color(0xFFFFAB00)
val AmberSubtle    = Color(0x26FFAB00)

// Accent — Rose (error / danger)
val RoseCore       = Color(0xFFFF4D6D)
val RoseSubtle     = Color(0x26FF4D6D)

// System color-coding gradients
val SystemNeuro    = Color(0xFF8B5CF6) // purple  — nervous
val SystemCardio   = Color(0xFFEF4444) // red     — cardiovascular
val SystemRespire  = Color(0xFF3B82F6) // blue    — respiratory
val SystemDigest   = Color(0xFFF59E0B) // amber   — digestive
val SystemMusculo  = Color(0xFF10B981) // green   — musculoskeletal
val SystemEndocrin = Color(0xFFEC4899) // pink    — endocrine
val SystemRenal    = Color(0xFF06B6D4) // cyan    — renal
val SystemImmune   = Color(0xFF84CC16) // lime    — immune
val SystemRepro    = Color(0xFFF97316) // orange  — reproductive
val SystemSkeletal = Color(0xFF94A3B8) // slate   — skeletal
val SystemLymph    = Color(0xFF14B8A6) // teal    — lymphatic
val SystemSkin     = Color(0xFFD97706) // yellow  — integumentary

// Text
val TextPrimary    = Color(0xFFE8F0FE)
val TextSecondary  = Color(0xFF8FA3BF)
val TextMuted      = Color(0xFF4A6080)
val TextOnAccent   = Color(0xFF04080F)

// Divider / border
val BorderSubtle   = Color(0x1AFFFFFF)
val BorderCard     = Color(0x33FFFFFF)

// Subscription tiers
val GoldPremium    = Color(0xFFFFD700)
val GoldSubtle     = Color(0x26FFD700)
val SilverFree     = Color(0xFF94A3B8)